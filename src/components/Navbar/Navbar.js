import React from 'react';

const Navbar = () => {
    return (
        <div>
            <button className='btn btn-primary'>jhakanaka</button>
            
        </div>
    );
};

export default Navbar;